  // // client sends command to move up - experimented with set interval which moves up every 50ms
  // conn.on("connect", () => {
  //   // setInterval(() => {
  //   //   conn.write("Move: up");;
  //   // }, 50);

  // });